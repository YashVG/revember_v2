# Optional. Defaults to <repo>/RevemberKnowledge.
# Set an absolute path only when the server should use a different knowledge store.
# REVEMBER_KNOWLEDGE_ROOT=/absolute/path/to/RevemberKnowledge

# Optional. Defaults to ~/Library/Application Support/RevemberV2/progress.json.
# REVEMBER_PROGRESS_PATH=/absolute/path/to/progress.json
